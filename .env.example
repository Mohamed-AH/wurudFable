# Server Configuration
NODE_ENV=development
PORT=3000

# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/duroos?retryWrites=true&w=majority

# Session Secret (generate a random string)
SESSION_SECRET=your-random-session-secret-here

# Google OAuth 2.0
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Admin Email Whitelist (comma-separated)
ADMIN_EMAILS=admin@example.com,another-admin@example.com

# File Upload Configuration (local - optional if using OCI)
UPLOAD_DIR=/mnt/audio
MAX_FILE_SIZE=62914560

# Production URL (update for production)
SITE_URL=http://localhost:3000

# Oracle Cloud Infrastructure (OCI) Object Storage
# Required for audio file storage on OCI Always Free tier
OCI_NAMESPACE=your-tenancy-namespace
OCI_BUCKET=wurud-audio
OCI_REGION=us-ashburn-1
OCI_TENANCY=ocid1.tenancy.oc1..xxxxx
OCI_USER=ocid1.user.oc1..xxxxx
OCI_FINGERPRINT=xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx
OCI_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
...your private key content here...
-----END RSA PRIVATE KEY-----"

# Transcript Search Configuration
# Separate MongoDB connection for search database (transcripts collection)
SEARCH_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/samplesearch?appName=Cluster0
SEARCH_MODE=atlas
CONTEXT_WINDOW_SEC=45
CONTEXT_ITEMS=2
LOG_SEARCHES=true
SEARCH_LOG_TTL_DAYS=30

# Grafana Cloud Prometheus Monitoring (optional)
# Push metrics to Grafana Cloud for OOM tracking
# Use the Influx endpoint (not /api/prom/push which requires protobuf)
GRAFANA_URL=https://influx-prod-xx-prod-xx-xxxxx.grafana.net/api/v1/push/influx/write
GRAFANA_USER_ID=123456
GRAFANA_API_TOKEN=glc_your_api_key_here
# Metric instance tag to differentiate servers in Grafana (e.g., "stable", "test")
# Also used as Sentry environment identifier
METRIC_TAG=stable

# Sentry Error Tracking & Performance Monitoring
# Create a Node.js project at https://sentry.io and copy the DSN
SENTRY_DSN=https://xxxxxxxx@oxxxxxx.ingest.sentry.io/xxxxxxxx
# Sample rates (0.0 to 1.0) - 1.0 recommended for low-traffic sites (<100 users/week)
SENTRY_TRACES_RATE=1.0
SENTRY_PROFILES_RATE=1.0
